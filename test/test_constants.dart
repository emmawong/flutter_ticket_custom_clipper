import 'package:flutter/material.dart';

/// Constants used across test files for consistent test setup
class TestConstants {
  /// Extra small ticket size
  static const extraSmallSize = (90.0, 40.0);

  /// Small ticket size
  static const smallSize = (190.0, 80.0);

  /// Normal ticket size
  static const normalSize = (290.0, 120.0);

  /// Surface size for golden test
  static const goldenTestSurfaceSize = Size(400, 200);
}
